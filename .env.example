# Musicr Production Environment Configuration
# Copy this file to .env and update the values for your deployment

# Database Configuration
POSTGRES_PASSWORD=your-secure-postgres-password-here

# API Configuration
OPENAI_API_KEY=sk-your-openai-api-key-here
COOKIE_SECRET=your-secure-cookie-secret-here

# Application Environment
NODE_ENV=production
LOG_LEVEL=info

# Frontend Configuration
VITE_API_URL=http://localhost:4000

# Optional: Performance Tuning
NODE_OPTIONS=--max-old-space-size=512

# Optional: Custom Ports (if defaults conflict)
# DATABASE_PORT=5432
# API_PORT=4000
# WEB_PORT=5173

# Optional: External Database (if not using Docker Postgres)
# For Railway or external PostgreSQL, use sslmode=require for production
# DATABASE_URL=postgresql://user:password@host:port/database?schema=public&sslmode=require

# Optional: Security Headers
# CORS_ORIGIN=http://localhost:5173,https://yourdomain.com

# Optional: Logging Configuration
# LOG_FILE=/app/logs/musicr.log
# LOG_ROTATION=daily

# Deployment Notes:
# 1. Generate a secure POSTGRES_PASSWORD (32+ characters)
# 2. Add your OpenAI API key for embedding functionality
# 3. Generate a secure COOKIE_SECRET (64+ characters)
# 4. Update VITE_API_URL if deploying to different host/port
# 5. Consider firewall rules for ports 4000 (API) and 5173 (Web)